/**
 * C0ntext API
 * 1. create context export
 * 2. create context provider
 * 3. set context value
 * 4. to access the value : useContext hook
 */

/**
 * 1. main.jsx set AuthProvider
 * 2. add children props inside AuthProvider
 */

/**
 * 1. break down the create user function
 * 2. auth provider will have the auth and pass email and pass from register
 */